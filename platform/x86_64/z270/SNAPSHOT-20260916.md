# Z270 dual-zone source checkpoint — 2026-09-16

Branch: `feat/z270-zone1-virtio-blk-backend`.
This checkpoint preserves the previously uncommitted real-hardware adaptation;
it is not a new deployment or a claim that every local edit is in the live image.

Changes include guest-only ACPI PM emulation, split USB ownership, PCI routing,
CPU-only backend guest-RAM aliases excluded from VT-d mappings, and the bounded
Zone1 virtio block/net staging windows. Zone0 remains a trusted backend owner.
Guest root storage is virtio-blk, not SATA-controller passthrough.

Last real-machine validation (2026-09-15): two Arch systems with independent
NVIDIA/Intel displays and USB, guest disk-backed Plasma, bidirectional private
network/SSH and IPv4/IPv6 Internet egress. Zone1 kernel remains pinned at
6.18.50-2-lts with its patched i915 module. Do not substitute native i915.

This checkpoint ran `python3 platform/x86_64/z270/test-device-contract.py`
(PASS) and `git diff --check`. No rebuild/reboot in this backup-only operation.
The latest SSH attempt on 2026-09-16 timed out, so live hashes were not freshly
verified. Recovery scripts and validation history are backed up separately in
the user's private zone-shmnet repository under `recovery/z270-20260916/`.

Companion backend: xjimlinx/hvisor-tool, same branch name. Existing historical
branches are retained; neither upstream default branch is overwritten.
